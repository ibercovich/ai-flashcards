A feature of neural networks where each neuron in a layer is connected to only a small region of the input, assuming nearby pixels are more relevant than distant ones, commonly used in CNNs.
---
Local Connectivity
